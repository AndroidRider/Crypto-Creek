<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CryptoCreek" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->

        <item name="android:statusBarColor">@color/statusBarColor</item>
        <item name="fontFamily">@font/nunito_medium</item>

        <!-- Changing the status bar item color white to black -->
        <item name="android:windowLightStatusBar">true</item>

    </style>

    <style name="Theme.CryptoCreek" parent="Base.Theme.CryptoCreek" />
</resources>